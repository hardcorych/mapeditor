#pragma once

#include <Editor.h>

class BlockEditor : public Editor
{
public:
  BlockEditor() = default;
  ~BlockEditor() = default;
  
private:
  //addTexture
  //setPassability
  //saveBlock ?
  //loadBlock ?
};